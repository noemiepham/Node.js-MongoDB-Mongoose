import React from 'react';

import useThanos from '../useThanos/index.js';

export default ({ children }) => {
  const snap = useThanos();
  return (
    <>
      <button onClick={() => snap()}>snap</button>
      {children}
    </>
  );
};
